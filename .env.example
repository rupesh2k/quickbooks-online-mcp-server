# QuickBooks Online API Credentials
# Get these from https://developer.intuit.com/

# Client ID from your Intuit Developer app
QUICKBOOKS_CLIENT_ID=your_client_id_here

# Client Secret from your Intuit Developer app
QUICKBOOKS_CLIENT_SECRET=your_client_secret_here

# Environment: 'sandbox' for testing, 'production' for live data
QUICKBOOKS_ENVIRONMENT=sandbox

# Redirect URI for OAuth callback (must match Intuit Developer Portal)
QUICKBOOKS_REDIRECTURI=http://localhost:8000/callback

# OAuth Refresh Token (OPTIONAL - managed automatically)
# Leave empty on first run - the app will prompt for OAuth authentication
# Once authenticated, tokens are automatically stored in .qb-tokens.json
# and refreshed as needed. You don't need to manually update this!
QUICKBOOKS_REFRESH_TOKEN=

# Realm ID (Company ID) from QuickBooks (OPTIONAL - managed automatically)
# Leave empty on first run - obtained automatically during OAuth flow
QUICKBOOKS_REALM_ID=

# Optional: Node environment
NODE_ENV=development
